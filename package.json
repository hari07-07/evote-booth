{
  "name": "evote-bridge-v2",
  "version": "2.0.0",
  "description": "EVote Bridge - Secure Cross-Booth Voter Verification System",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp -r * public/ 2>/dev/null || true",
    "test": "echo 'No tests currently written' && exit 0"
  },
  "dependencies": {
    "resend": "^3.2.0",
    "@supabase/supabase-js": "^2.39.0",
    "otplib": "^12.0.1",
    "qrcode": "^1.5.3"
  }
}
